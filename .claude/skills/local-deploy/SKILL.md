---
description: Start the local Docker dev environment (api + frontend containers)
user-invocable: true
---

Run `./scripts/build.sh up` in the background to start the api and frontend Docker containers for local development, then confirm all containers are healthy.
